name: Build ZMK Firmware

on: [push, pull_request, workflow_dispatch]

jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@v3
    with:
      build_matrix: |
        [
          { "board": "nice_nano_v2", "shield": "corne_left" },
          { "board": "nice_nano_v2", "shield": "corne_right" }
        ]
